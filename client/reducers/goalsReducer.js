const initialState = {};

const goalsReducer = (state = initialState, action) => {
    // switch (action.type {
    //     case "name of action" :
    //         return // [...state, update to state ]
    // })
    switch (action.type) {
        default:
            return state;
    }
};

export default goalsReducer